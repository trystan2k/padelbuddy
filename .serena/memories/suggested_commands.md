# Padel Buddy – Suggested Commands

## Development
- `npm run test` – run test suite (QA gate)
- `zeus dev` – run in simulator with hot reload
- `zeus preview` – generate QR code for real device testing
- `zeus build` – build app (.zab)

## System utilities (Darwin)
- `ls`, `find`, `grep`, `git`, `npm`, `node`
