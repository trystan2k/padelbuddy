## Goal
Produce a deepthink execution plan for Task 76 (LocalStorage-first persistence migration) aligned to the updated Zepp OS 3.6+ only scope.

## Instructions
- Use the user override instead of the original backward-compatibility scope.
- No legacy support, no legacy data migration, no v1 API support.
- Write the plan file into docs/plan and return only the plan path, ordered plan summary, and scope adjustments.

## Discoveries
- Current persistence is split across `utils/storage.js`, `utils/active-session-storage.js`, `utils/match-storage.js`, `utils/match-history-storage.js`, and `utils/haptic-feedback-settings.js`.
- `utils/platform-adapters.js` already exposes a generic `storage` adapter that resolves `__zosStorage` / `localStorage` / `LocalStorage` and is a strong fit for a shared `utils/persistence.js` core.
- `app.js` still runs `migrateLegacySessions()` at startup, and many tests still depend on `hmFS` helpers.
- `page/game/persistence.js` still contains a `schemaVersion: 1` fallback that should be updated during the storage migration.

## Accomplished
- Reviewed relevant memories, manifest state, PRD/docs, and storage modules.
- Audited current storage consumers in pages/utilities and identified legacy-only areas to remove.
- Wrote the plan file at `docs/plan/Plan 76 Migrate Persistence to LocalStorage-First Strategy with Schema Migration.md`.

## Next Steps
- Implement `utils/persistence.js` on top of `platform-adapters.storage`.
- Refactor session/history/settings utilities onto the shared LocalStorage-first core.
- Remove startup legacy migration and `hmFS`/legacy test infrastructure after the new path is validated.

## Relevant Files
- docs/plan/Plan 76 Migrate Persistence to LocalStorage-First Strategy with Schema Migration.md — implementation-ready Task 76 plan
- utils/platform-adapters.js — modern storage adapter from Task 75
- utils/active-session-storage.js — main legacy cleanup target
- utils/match-storage.js — session facade to preserve for page callers
- utils/match-history-storage.js — history persistence to move off `hmFS`
- utils/haptic-feedback-settings.js — settings persistence to move off `hmFS`
- app.js — startup migration hook to remove